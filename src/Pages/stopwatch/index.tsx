import React from 'react';
import Stopwatch from '../../Components/stopwatch/stopwatch';

const WatchStop = () => {
  return (
    <div>
      <Stopwatch />
    </div>
  );
};

export default WatchStop;
